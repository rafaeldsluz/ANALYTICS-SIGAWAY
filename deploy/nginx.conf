server {
    listen 80;
    server_name SEU_DOMINIO_OU_IP;

    # Redireciona HTTP → HTTPS (descomente após configurar SSL com certbot)
    # return 301 https://$host$request_uri;
    
    
    location / {
        proxy_pass         http://127.0.0.1:5000;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;

        # Necessário para Server-Sent Events (SSE)
        proxy_buffering    off;
        proxy_cache        off;
        proxy_read_timeout 3600s;
        chunked_transfer_encoding on;
    }

    # Arquivos estáticos servidos diretamente pelo nginx
    location /static/ {
        alias /opt/sigaway/web/static/;
        expires 1d;
    }
}

# Bloco HTTPS — gerado automaticamente pelo certbot
# server {
#     listen 443 ssl;
#     server_name SEU_DOMINIO;
#     ssl_certificate     /etc/letsencrypt/live/SEU_DOMINIO/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/SEU_DOMINIO/privkey.pem;
#     ...
# }
